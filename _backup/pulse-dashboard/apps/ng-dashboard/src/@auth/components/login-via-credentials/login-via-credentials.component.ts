import { Component } from '@angular/core';

@Component({
  standalone: false,
  selector: 'app-login-via-credentials',
  template: '<p>Login via credentials</p>',
})
export class LoginViaCredentialsComponent {}
